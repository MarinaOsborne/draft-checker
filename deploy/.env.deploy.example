# Скопируй в deploy/.env.deploy и впиши свои значения.
# Этот файл (без .example) в гит не попадает — см. .gitignore.

VIBE_KEY=vibe_api_...
OPENAI_API_KEY=sk-...
ACCESS_PIN=2847

# Опционально — модель по умолчанию gpt-4o, можно переопределить:
# OPENAI_MODEL=gpt-4o
